---
type: code
status: completed
target_files:
- src/config.rs
- docs/concepts/prompts.md
- docs/reference/config.md
- CHANGELOG.md
commits:
- 8988467
model: haiku
---
# Make bootstrap prompt the default, update docs, prepare release

## Problem

The bootstrap prompt reduces API concurrency issues by starting with a minimal prompt that fetches spec content via `chant prep`. This should be the default behavior.

## Solution

1. Change default prompt from "standard" to "bootstrap" in config.rs
2. Update documentation to reflect bootstrap as default
3. Document the change in CHANGELOG.md for v0.3.2 release

## Acceptance Criteria

- [x] `defaults.prompt` defaults to "bootstrap" instead of "standard"
- [x] docs/concepts/prompts.md updated to show bootstrap as default
- [x] docs/reference/config.md updated
- [x] CHANGELOG.md updated with v0.3.2 changes (bootstrap prompt, prep command, jitter)
- [x] `just lint` passes