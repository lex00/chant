---
type: code
status: completed
commits:
- 705716f
completed_at: 2026-02-02T22:28:54Z
model: sonnet
---
# Add show --raw flag to replace prep command functionality

## Acceptance Criteria

- [x] Add `--raw` flag to `show` command
- [x] Add `--clean` flag to `show` command (works with `--raw`)
- [x] `show --raw` outputs just the spec body (no frontmatter, no formatting)
- [x] `show --raw --clean` strips agent conversation sections
- [x] Function matches `prep` command behavior exactly
- [x] Help text documents the new flags
- [x] Code is formatted with `cargo fmt`
- [x] Code passes `cargo clippy`

## Agent Output

2026-02-02T22:28:54Z

```
I'll implement the task to add `--raw` flag to `show` command to replace `prep` functionality.
## Summary

Added `--raw` and `--clean` flags to `show` command in src/cmd/spec/show.rs:103, src/main.rs:157-171.

- `show --raw` outputs spec body without frontmatter or formatting
- `show --raw --clean` strips agent conversation sections
- Made `strip_agent_conversation` public in src/cmd/prep.rs:21
- Behavior matches `prep` command exactly```
