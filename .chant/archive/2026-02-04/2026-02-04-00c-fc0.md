---
type: code
status: completed
branch: chant/2026-02-04-00c-fc0
commits:
- '212e120'
completed_at: 2026-02-04T18:41:19Z
model: sonnet
---
# Add skip_criteria parameter to chant_work_start MCP tool

## Problem

MCP `chant_work_start` fails on specs with quality issues because it runs non-interactively and there's no way to pass `--skip-criteria`.

## Solution

In `src/mcp/handlers.rs`:

1. Add `skip_criteria` boolean to inputSchema (line ~291)
2. Parse it in `tool_chant_work_start` (line ~1645)
3. Pass it to the work command args

## Acceptance Criteria

- [x] Add `"skip_criteria": {"type": "boolean"}` to chant_work_start inputSchema at line ~291
- [x] Parse `skip_criteria` from args in `tool_chant_work_start` at line ~1645
- [x] Append `--skip-criteria` to cmd args when true

## Agent Output

2026-02-04T18:41:19Z

```
I'll implement the changes to add the `skip_criteria` parameter to the `chant_work_start` MCP tool.
## Summary

Modified `src/mcp/handlers.rs` to add `skip_criteria` parameter support to the `chant_work_start` MCP tool.

- Added `skip_criteria` boolean to the inputSchema at line 303
- Parsed `skip_criteria` from args at line 1651
- Append `--skip-criteria` to command args when true at line 1682

All acceptance criteria met and changes committed.```
