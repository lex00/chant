---
type: code
status: completed
branch: chant/2026-02-04-00d-hoi
commits:
- dbd579f
completed_at: 2026-02-04T18:46:48Z
model: sonnet
---
# Add optional spec ID argument to chant lint command for single-spec validation

## Problem

`chant lint` only validates all specs. Users need to lint a single spec by ID.

## Solution

Add optional positional argument `[SPEC_ID]` to lint command. If provided, only lint that spec.

## Acceptance Criteria

- [x] Add `spec_id: Option<String>` argument to Lint command in `src/main.rs`
- [x] In `cmd_lint`, if spec_id provided, resolve and lint only that spec
- [x] `chant lint abc` lints only spec matching "abc"

## Agent Output

2026-02-04T18:46:48Z

```
I'll implement the optional spec ID argument for the chant lint command.
## Summary

Modified src/main.rs and src/cmd/spec/lint.rs to add optional spec ID filtering to the lint command.

- Added `spec_id: Option<String>` parameter to Lint command struct
- Updated cmd_lint signature to accept optional spec_id parameter  
- When spec_id provided, uses resolve_spec to lint only that single spec
- Maintains full backward compatibility for linting all specs

All acceptance criteria completed. Code formatted with cargo fmt, passed clippy checks, and verified working with test runs.```
