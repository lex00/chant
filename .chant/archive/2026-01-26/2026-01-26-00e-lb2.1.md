---
type: code
status: completed
target_files:
- src/main.rs
completed_at: 2026-01-26T23:30:19Z
model: haiku
---
# Add verify subcommand structure and basic argument parsing

Add the `verify` subcommand to the CLI with argument parsing for all flags and options. This establishes the command interface without implementing the verification logic yet. The command should parse arguments correctly and print a placeholder message indicating it's not yet implemented.

### Acceptance Criteria

- [ ] `Verify` variant added to `Commands` enum in `main.rs`
- [ ] Clap arguments defined: optional `[ID]`, `--all`, `--label`, `--exit-code`, `--dry-run`, `--prompt`
- [ ] `handle_verify()` function stub created that parses arguments and prints "Not yet implemented"
- [ ] `just chant verify --help` shows all options with descriptions
- [ ] Code compiles with `just build` and passes `just lint`

### Edge Cases

- Multiple flags combined: `chant verify --all --label foo --dry-run` should parse without errors
- Invalid spec ID format: Should use existing ID validation patterns from other commands
- Conflicting options: `chant verify 001 --all` should be allowed (ID takes precedence per typical CLI behavior)

### Example Test Cases

For this feature, verify:
- `chant verify` with no args should show help or default to `--all` behavior (document chosen behavior)
- `chant verify 001` should parse ID "001" correctly
- `chant verify --label urgent --exit-code` should parse both flags