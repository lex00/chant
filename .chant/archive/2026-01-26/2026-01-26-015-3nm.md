---
type: code
status: completed
target_files:
  - src/main.rs
  - docs/reference/cli.md
model: claude-haiku-4-5-20251001
---

# Add interactive wizard mode to chant init command - guide new users through setup

## Overview

When `chant init` is run without any flags, launch an interactive wizard to guide new users through project setup.

## Interface

```bash
chant init                             # Wizard mode
chant init --name myproject            # Direct mode (existing)
chant init --agent claude --minimal    # Direct mode (existing)
```

### Wizard Flow

```
? Project name: my-project (auto-detected)
? Include prompt templates? Yes
? Keep .chant/ local only (gitignored)? No
? Initialize agent configuration?
› None
  Claude Code (CLAUDE.md)
  Cursor (.cursorrules)
  Amazon Q (.amazonq/rules.md)
  Generic (agent.md)
  All of the above

Initializing chant...
Created .chant/config.md
Created .chant/prompts/standard.md
Created .chant/prompts/tdd.md
Created .chant/specs/
Created CLAUDE.md
Done! Run 'chant add "description"' to create your first spec.
```

## Implementation

- Detect if running with no flags (wizard mode) vs with flags (direct mode)
- Use `dialoguer::Input` for project name (with detected default)
- Use `dialoguer::Confirm` for prompt templates (default: yes)
- Use `dialoguer::Confirm` for silent mode (default: no)
- Use `dialoguer::Select` for agent selection with options:
  - None
  - Claude Code
  - Cursor
  - Amazon Q
  - Generic
  - All
- Call existing init logic with gathered values

## Acceptance Criteria

- [x] `chant init` (no args) launches wizard
- [x] Wizard prompts for: project name, templates, silent, agent
- [x] Auto-detected project name shown as default
- [x] Direct mode flags still work unchanged
- [x] Tests for wizard flow
- [x] Documentation updated
