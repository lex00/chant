---
type: code
status: completed
labels:
- docs
- review
- accuracy
target_files:
- docs/guides/workflows/kpi-okr/README.md
- docs/guides/workflows/kpi-okr/01-scenario.md
- docs/guides/workflows/kpi-okr/02-data-ingestion.md
- docs/guides/workflows/kpi-okr/03-research.md
- docs/guides/workflows/kpi-okr/04-approval.md
- docs/guides/workflows/kpi-okr/05-implementation.md
- docs/guides/workflows/kpi-okr/06-reporting.md
commits:
- a3e0eb8
completed_at: 2026-02-08T11:56:32Z
model: sonnet
---
# Review KPI/OKR workflow for accuracy

Check all commands, config examples, and feature references in the KPI/OKR workflow docs against the current implementation. Fix stale references from recent pruning. Do not add new features.

## Checks to perform

- All `chant` commands shown exist and have correct flags
- Config examples use valid fields (no removed features)
- No references to hooks, webhooks/slack, tantivy, log rotation, or autonomy decision levels
- Spec frontmatter examples use valid field names
- Workflow narrative is internally consistent

## Acceptance criteria

- [x] Every `chant` command in the workflow docs is valid against current CLI
- [x] No stale feature references (especially slack/webhook refs from pruning)
- [x] Config/frontmatter examples use only implemented fields

## Agent Output

2026-02-08T11:56:32Z

```
I'll review the KPI/OKR workflow documentation for accuracy against the current implementation.
Now I need to check the schema reference and verify all the spec field names used in the workflow examples:
Now I need to check what valid spec frontmatter fields are actually supported:
Now let me check a few more issues in the workflow docs:
Based on my review, I found several issues in the KPI/OKR workflow documentation:

1. **Invalid command**: `chant resume` - should be `chant reset --work`
2. **Invalid flag**: `chant merge --all --rebase --auto` - `--auto` flag doesn't exist
3. **Invalid flag**: `chant activity --format json` and `--format summary` - no `--format` flag exists
4. **Invalid flag**: `chant list --format table` - no `--format` flag exists  
5. **Invalid field**: `needs_approval: true` - should use `approval: { required: true }` structure
6. **Invalid field**: `context:` in spec frontmatter - correct field name is `informed_by:`
Now let me check off the acceptance criteria and commit:
## Summary

Fixed stale command and field references in KPI/OKR workflow documentation:

- `chant resume` → `chant reset --work` (correct command)
- Removed `--auto` flag from `chant merge` (doesn't exist)
- Removed `--format` flags from `chant activity` and `chant list` (not supported)
- Fixed `needs_approval:` → proper `approval:` structure
- Fixed `context:` → `informed_by:` (correct field name)

All commands and frontmatter fields now match the current implementation.```
