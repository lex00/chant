---
type: code
status: pending
depends_on:
- 2026-01-24-01e-o0l.2
target_files:
- src/main.rs
---
Update both `cmd_work()` and `cmd_work_parallel()` to explicitly check the result of `spec.save()` and propagate errors. In parallel mode, currently save errors are silently swallowed with `let _ = spec.save()`. Make these failures visible and prevent silent spec state corruption.
