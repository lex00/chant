---
type: code
status: blocked
depends_on:
- 2026-02-07-00y-qqd
labels:
- operations
- architecture
target_files:
- src/operations/pause.rs
- src/operations/verify.rs
- src/mcp/handlers.rs
---
# Add pause and verify operations to operations layer

## Context

Pause and verify are implemented inline in both MCP handlers and CLI commands:

- **MCP pause** (`handlers.rs:2159-2221`): inline PID management + status change
- **CLI pause** (`cmd/pause.rs`): separate PID management with `--force` flag behavior
- **MCP verify** (`handlers.rs` — `tool_chant_verify`): inline verification logic
- **CLI verify** (`cmd/verify.rs`): full verification with detailed output

## Task

Create `src/operations/pause.rs` (with `PauseOptions` and `pause_spec()`, handling PID management) and `src/operations/verify.rs` (with `VerifyOptions` and `verify_spec()`, with verification runner logic). Update both MCP handlers (`tool_chant_pause`, `tool_chant_verify`) and CLI commands to call the new operations, then re-export from `src/operations/mod.rs`.

## Acceptance Criteria

- [ ] `src/operations/pause.rs` exists with `pause_spec()` used by both MCP and CLI
- [ ] `src/operations/verify.rs` exists with `verify_spec()` used by both MCP and CLI
- [ ] No inline pause/verify logic remains in MCP handlers
- [ ] `cargo test` passes
- [ ] `cargo clippy -- -D warnings` passes