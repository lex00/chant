---
type: code
status: pending
depends_on: []
target_files:
- src/cmd/lifecycle/mod.rs
- src/cmd/lifecycle/merge.rs
- src/cmd/lifecycle/archive.rs
- src/cmd/lifecycle/drift.rs
- src/cmd/lifecycle/resume.rs
- src/cmd/lifecycle/split.rs
- src/cmd/mod.rs
- src/cmd/lifecycle.rs
---
# Split cmd/lifecycle.rs into focused modules

Extract the 4216-line lifecycle.rs into a module directory with 6 submodules (merge, archive, drift, resume, split, mod).

### Inherited Context

- Each task must leave code in a compilable state
- Changes should be independently testable
- Each submodule should be under 800 lines

### Acceptance Criteria

- [ ] Create src/cmd/lifecycle/ directory with mod.rs and 5 submodules
- [ ] Move functions to appropriate submodules (merge, archive, drift, resume, split)
- [ ] Update src/cmd/mod.rs to reference new module path
- [ ] Delete original src/cmd/lifecycle.rs
- [ ] cargo build succeeds and cargo test passes

### Edge Cases

- Public/private function visibility must be preserved
- Module re-exports must match original public API
- Internal cross-function calls must use correct module paths

### Example Test Cases

- Existing lifecycle tests should pass without modification
- All public functions accessible via src::cmd::lifecycle::*
- cargo clippy should not warn about unused imports