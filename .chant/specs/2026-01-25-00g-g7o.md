---
type: code
status: pending
target_files:
- src/main.rs
- templates/claude.md
- templates/cursorrules.md
- templates/amazonq.md
- templates/ai-instructions.md
---
# Implement chant init --agent for multiple AI providers

## Overview

Implement the `chant init --agent` command with support for multiple AI coding assistants.

## Supported Providers

| Provider | Config File | Notes |
|----------|-------------|-------|
| Claude Code | CLAUDE.md | Project instructions for Claude |
| Cursor | .cursorrules | Cursor IDE rules |
| Amazon Q | .amazonq/rules.md | Amazon Q Developer |
| Generic | .ai-instructions | Portable fallback |

## Implementation Tasks

### 1. Add templates directory
- [ ] Create `templates/` directory in source
- [ ] Add `claude.md` template (CLAUDE.md content)
- [ ] Add `cursorrules.md` template
- [ ] Add `amazonq.md` template
- [ ] Add `ai-instructions.md` template
- [ ] Embed templates using `include_str!()` macro

### 2. Update CLI
- [ ] Add `--agent` flag to `init` command
- [ ] Add `--agent-type` option (claude, cursor, amazonq, generic, all)
- [ ] Interactive mode if no type specified

### 3. Template content
Each template should include:
- Chant workflow explanation (specs, not direct coding)
- Key commands (`just chant add`, `just chant work`, etc.)
- What NOT to do (no direct file edits)
- Provider-specific formatting

### 4. Init logic
- [ ] Detect existing files (don't overwrite without --force)
- [ ] Create directories if needed (.amazonq/)
- [ ] Write templates to correct locations
- [ ] Print success message with next steps

## Acceptance Criteria

- [ ] `chant init --agent` launches interactive wizard
- [ ] `chant init --agent claude` creates CLAUDE.md
- [ ] `chant init --agent cursor` creates .cursorrules
- [ ] `chant init --agent amazonq` creates .amazonq/rules.md
- [ ] `chant init --agent generic` creates .ai-instructions
- [ ] `chant init --agent all` creates all config files
- [ ] Templates embedded in binary (no network fetch)
- [ ] Existing files not overwritten without --force
- [ ] Success message shows next steps
- [ ] All tests pass
