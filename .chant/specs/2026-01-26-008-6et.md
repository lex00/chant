---
type: code
status: completed
target_files:
- src/cmd/spec.rs
model: claude-opus-4-5
---

# Add unit tests for lint validation of documentation and research spec types

Test that lint warns when documentation spec missing tracks, warns when documentation spec missing target_files, warns when research spec missing both informed_by and origin, warns when research spec missing target_files.

## Implementation

Extracted `validate_spec_type` helper function from `cmd_lint` to enable direct unit testing of validation logic.

## Acceptance Criteria

- [x] Test documentation spec missing `tracks` produces warning
- [x] Test documentation spec missing `target_files` produces warning
- [x] Test documentation spec missing both produces two warnings
- [x] Test valid documentation spec produces no warnings
- [x] Test research spec missing both `informed_by` and `origin` produces warning
- [x] Test research spec missing `target_files` produces warning
- [x] Test valid research spec with `origin` produces no warnings
- [x] Test valid research spec with `informed_by` produces no warnings
- [x] Test valid research spec with both produces no warnings
- [x] Test code/task specs produce no type-specific warnings
- [x] All tests pass
