---
type: documentation
status: completed
target_files:
- docs/reference/config.md
commits:
  - 9cd1d30
completed_at: 2026-01-24T12:44:24Z
---
# Document global config at ~/.config/chant/

Add documentation for global config support (implemented in spec 007-hzq).

## Acceptance Criteria

- [x] Document global config location: ~/.config/chant/config.md
- [x] Explain merge behavior (project overrides global)
- [x] Show example global config
- [x] Show example of project config overriding global defaults

## Example Content

### Global Configuration

Chant supports a global config file for user-wide defaults:

```
~/.config/chant/config.md
```

### Merge Behavior

Project config overrides global config:

```
~/.config/chant/config.md    <- Global defaults
.chant/config.md             <- Project overrides
```

### Example Global Config

```markdown
# ~/.config/chant/config.md
---
defaults:
  branch: true
  pr: true

git:
  provider: github
---

# Global Chant Settings

My default settings for all projects.
```

### Example Project Override

```markdown
# .chant/config.md
---
project:
  name: quick-prototype

defaults:
  branch: false   # Override: direct commits for this project
  pr: false
---
```

## Implementation Notes

- Update docs/reference/config.md
- Add new section "Global Configuration"
- Explain that global config uses same format as project config