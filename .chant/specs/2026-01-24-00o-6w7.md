---
type: documentation
status: in_progress
target_files:
- docs/reference/mcp.md
---
# Document chant mcp command usage

Add documentation for the `chant mcp` command that runs the MCP server (implemented in specs 004-006).

## Acceptance Criteria

- [x] Document the `chant mcp` subcommand
- [x] Show how to test the MCP server manually
- [x] List the three implemented tools with accurate schemas
- [x] Remove references to unimplemented tools (chant_commit, chant_verify)
- [x] Show example JSON-RPC requests and responses

## Example Content

```bash
# Start MCP server (reads from stdin, writes to stdout)
chant mcp

# Test with JSON-RPC request
echo '{"jsonrpc":"2.0","method":"tools/list","id":1}' | chant mcp
```

### Implemented Tools

| Tool | Description | Parameters |
|------|-------------|------------|
| `chant_spec_list` | List specs | `status` (optional) |
| `chant_spec_get` | Get spec details | `id` (required, partial match supported) |
| `chant_spec_update` | Update spec | `id` (required), `status`, `output` (optional) |

## Implementation Notes

- Update docs/reference/mcp.md
- Remove chant_commit and chant_verify from tool list (not implemented)
- The command is `chant mcp` not `chant mcp-server`