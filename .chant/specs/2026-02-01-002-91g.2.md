---
type: code
status: pending
depends_on:
- 2026-02-01-002-91g.1
target_files:
- src/cmd/commits.rs
---
# Update cmd/commits.rs to use consolidated git functions

Refactor cmd/commits.rs to call git.rs functions instead of executing raw git commands directly.

### Inherited Context

- Each task must leave code in a compilable state
- Maintain existing behavior while improving structure
- Remove duplicate Command::new("git") calls

### Acceptance Criteria

- [ ] Replace direct git Command calls with git.rs functions
- [ ] Remove all Command::new("git") log operations
- [ ] Existing tests pass with no behavior changes
- [ ] cargo build succeeds