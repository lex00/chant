---
type: code
status: in_progress
---
# Remove defaults.branch config option from src/config.rs

## Acceptance Criteria

- [x] Remove `branch: bool` field from `DefaultsConfig` struct
- [x] Remove `branch` field initialization from `DefaultsConfig::default()`
- [x] Remove `branch` field from `PartialDefaultsConfig` struct
- [x] Remove `branch` field merge logic from `PartialConfig::merge_with()`
- [x] Update `src/cmd/work/chain.rs` to not check `config.defaults.branch`
- [x] Update `src/cmd/work/parallel.rs` to not check `config.defaults.branch`
- [x] Update tests to remove assertions about `config.defaults.branch`
- [x] Run `cargo fmt` to format code
- [x] Run `cargo clippy` with no warnings
- [x] Run `just test` with all tests passing