---
type: code
status: completed
target_files:
- src/mcp.rs
commit: 4c6d740
completed_at: 2026-01-24T04:34:21Z
---
# Add chant_spec_update tool to MCP server

Add the `chant_spec_update` tool to allow agents to update spec status and add output.

## Acceptance Criteria

- [ ] Add `chant_spec_update` tool to the tools/list response
- [ ] Implement the tool to update spec status
- [ ] Accept `id` parameter (required) for spec identification
- [ ] Accept `status` parameter (optional) to update spec status
- [ ] Accept `output` parameter (optional) to append to spec body
- [ ] Save changes to the spec file

## Tool Schema

```json
{
  "name": "chant_spec_update",
  "description": "Update a chant spec status or add output",
  "inputSchema": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Spec ID (full or partial)"
      },
      "status": {
        "type": "string",
        "description": "New status (pending, in_progress, completed, failed)"
      },
      "output": {
        "type": "string",
        "description": "Output text to append to spec body"
      }
    },
    "required": ["id"]
  }
}
```