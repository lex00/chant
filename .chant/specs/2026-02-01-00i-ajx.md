---
type: docs
status: pending
---

# Add missing status command flags to docs

The `status` command has flags `--brief`, `--json`, `--global`, `--repo` that are not documented in `docs/reference/cli.md`.

## Tasks

1. Read status command definition in src/main.rs (around line 264-280)
2. Update status section in `docs/reference/cli.md`
3. Document each flag with description and example
4. Show example output for `--brief` and `--json` modes

## Reference (from main.rs)

```rust
Status {
    #[arg(long)]
    global: bool,      // Show status across all repos
    #[arg(long)]
    repo: Option<String>, // Specific repo path
    #[arg(long)]
    watch: bool,       // Already documented
    #[arg(long)]
    brief: bool,       // Compact one-line output
    #[arg(long)]
    json: bool,        // JSON output format
}
```

## Acceptance Criteria

- [ ] `--brief` flag documented with example output
- [ ] `--json` flag documented with example JSON
- [ ] `--global` flag documented
- [ ] `--repo` flag documented
- [ ] All flags shown in command synopsis
